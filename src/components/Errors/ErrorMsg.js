import "../../styles/layout/errorMsg.scss";

const ErrorMsg = () => {
  return (
    <p className="errorMsg">
      We don't have this character in our collection yet. But, hey, try looking
      in another house!
    </p>
  );
};

export default ErrorMsg;
